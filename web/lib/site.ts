// Production origin. A literal, not an environment lookup: every
// absolute URL the site emits (metadataBase, canonical links, the
// Markdown twins) must resolve to the same host regardless of which
// Vercel deployment or local shell is rendering it.
export const SITE_ORIGIN = "https://agentcookie.dev";
